#include <stdlib.h>
#include "holberton.h"

/**
 * strtow - splits a string into words.
 * @str: string to split
 *
 * Return: array of strings.
 */

char **strtow(char *str)
{
	char **result = NULL;

	if (str == NULL)
		return (NULL);


	return (result);
}
